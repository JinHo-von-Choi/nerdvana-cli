# Phase G2 Retrospective — Hook Bridge + Sanitizer (v0.8.3)

작성자: 최진호
작성일: 2026-04-18

## 목표 달성 현황

| 목표 | 상태 |
|------|------|
| hook_bridge.py 전면 구현 | 완료 |
| hook_schemas.py TypedDict 모델 | 완료 |
| sanitizer.py 이중 게이트 | 완료 |
| audit.sqlite hooks 테이블 추가 | 완료 |
| main.py hook 서브커맨드 4종 | 완료 |
| 테스트 33개 신규 (목표 25+) | 완료 (749→782) |
| ruff clean | 완료 |
| mypy --strict clean | 완료 |
| parity 11 passed | 완료 |
| v0.8.3 태그 | 완료 |

## 신규 모듈

- `nerdvana_cli/server/hook_schemas.py`
- `nerdvana_cli/server/sanitizer.py`
- `nerdvana_cli/server/hook_bridge.py` (G1 placeholder → 전면 구현)

## Sanitizer 이중 게이트 설계

Gate-1 (블랙리스트 태깅):
- 7개 정규식 패턴 (ignore previous, disregard, you are now, system prompt:, special token, <<SYS>>, [INST])
- 매칭 시 `<!-- SANITIZED:pattern_id -->…<!-- /SANITIZED -->` 래핑 (차단 아님)

Gate-2 (구조 거부):
- `"role": "system"` JSON 패턴
- `<system>…</system>` XML 패턴
- `"function": {"name": …}` 도구 정의 패턴
- 매칭 시 빈 문자열 반환 + stderr 경고

민감 정보 마스킹:
- OpenAI 키, AWS AKID, 이메일, 고엔트로피 토큰

길이 제한: 4096자 초과 시 말미 절단 + [TRUNCATED]

## 발견된 이슈 및 해결

1. `"a" * 4096` 길이 테스트 실패 — 원인: TOKEN 패턴이 연속 알파 문자열에 매칭돼 redaction 후 짧아짐.
   해결: 테스트를 공백 구분 단어("hello ") 반복으로 변경.

2. main.py에서 `Path` 미임포트 — 해결: `from pathlib import Path` 추가.

3. ruff SIM108/SIM105/F841 — 해결: 삼항 연산자, contextlib.suppress, 미사용 변수 정리.

## AnchorMind 통합 상태

Phase G2에서는 placeholder만 구현. `nerdvana.yml.hooks.anchormind_inject: false` (기본값).
실제 recall 호출은 Phase G3+ 예정.

## 테스트 증분

```
749 (G1) → 782 (G2) : +33 tests
```

## 커밋 해시 (상세)

- feat(server): hook_bridge + hook_schemas — f0821ca
- feat(server): sanitizer + audit DDL — 8932b19
- feat(cli): hook subcommands — dd3813a
- test(server): 33 tests — c56746b
- chore: release 0.8.3 — (릴리즈 커밋)
