description: "Claude Code context — removes tools already built into Claude Code to avoid duplication"
prompt_override: null
prompt_append: "You are running inside Claude Code. File read/write and bash execution are handled by the host environment."
excluded_tools:
  - Bash
  - FileRead
  - FileWrite
  - FileEdit
  - Grep
  - Glob
included_tools: []
tool_description_overrides: {}
single_project: false
